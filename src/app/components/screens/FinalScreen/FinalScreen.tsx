import { StyledFinalScreenContainer } from './styled';
import { FinalScreenProps } from './types';

export const FinalScreen = ({ onClose }: FinalScreenProps) => {
  return <StyledFinalScreenContainer>...</StyledFinalScreenContainer>;
};
